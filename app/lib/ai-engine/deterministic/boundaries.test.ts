import test from "node:test";import assert from "node:assert/strict";import { readFileSync } from "node:fs";
test("public demo crawl has no persistence or model boundary",()=>{const route=readFileSync(new URL("../../../api/ai-builder/demo/crawl/route.ts",import.meta.url),"utf8");assert.doesNotMatch(route,/runModel|runOpenAi|repository|persistAiBuilder|persistWebsite|startCrawlTelemetry/);assert.match(route,/buildDeterministicBrain/);});
test("production intake assembles deterministically before persistence",()=>{const route=readFileSync(new URL("../../../api/ai-builder/intake/route.ts",import.meta.url),"utf8");const build=route.indexOf("assembleDeterministicSession");const persist=route.indexOf("persistAiBuilderProject",build);assert.ok(build>=0&&persist>build);assert.doesNotMatch(route,/runOpenAiIntakeModel|runEngine/);assert.match(route,/reconcileStructuredWebsiteKnowledge/);});
