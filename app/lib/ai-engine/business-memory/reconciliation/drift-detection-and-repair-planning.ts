import { detectDrift, type DriftItem, type ReconciliationState } from "./entity-reconciliation";
import type { TypedRepairOperation } from "./repair-executor";
export { detectDrift };
const operation: Record<string, TypedRepairOperation["type"]> = { missing_row: "insertResolvedEntity", unexpected_row: "deactivateResolvedEntity", stale_field: "updateResolvedEntity", wrong_entity_ownership: "assignSourceEntityOwner", broken_redirect: "repairRedirect", stale_identity_key: "updateIdentityKey", missing_alias: "insertAlias", stale_alias: "updateAlias", stale_assertion_owner: "assignAssertionOwner", missing_relationship: "rewireRelationship", stale_relationship_endpoint: "rewireRelationship", duplicate_relationship: "collapseDuplicateRelationship", missing_conflict: "insertConflict", stale_conflict: "updateConflict", source_evidence_lineage_mismatch: "repairEvidenceLink", revision_fingerprint_mismatch: "synchronizeFingerprint" };
const ranks: Record<TypedRepairOperation["type"], number> = { insertResolvedEntity: 0, updateResolvedEntity: 1, deactivateResolvedEntity: 20, assignSourceEntityOwner: 2, assignAssertionOwner: 3, insertIdentityKey: 4, updateIdentityKey: 5, deactivateIdentityKey: 21, insertAlias: 6, updateAlias: 7, preserveAcceptedAlias: 8, insertRedirect: 9, repairRedirect: 10, rewireRelationship: 11, collapseDuplicateRelationship: 12, insertConflict: 13, updateConflict: 14, deactivateConflict: 22, replaceConflictAssertionLinks: 15, repairSourceLink: 16, repairEvidenceLink: 17, synchronizeTrustedKnowledgeRevision: 18, synchronizeFingerprint: 99 };
export function buildTypedRepairPlan(drift: DriftItem[]): TypedRepairOperation[] { if (drift.some(x => !x.repairable)) throw new Error("business_memory_unrepairable_drift"); return drift.map(item => ({ type: operation[item.category] ?? "updateResolvedEntity", id: item.id, payload: item.expected })).sort((a,b) => ranks[a.type] - ranks[b.type] || a.id.localeCompare(b.id)); }
export const detectAndPlan = (desired: ReconciliationState, persisted: ReconciliationState) => buildTypedRepairPlan(detectDrift(desired, persisted));
